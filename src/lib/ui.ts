/**
 * Site-wide presentation constants.
 *
 * Keep every repeated class string here rather than in pages: a styling change
 * must be a one-line edit, not a hunt across six files.
 */

/**
 * The single content-link idiom. Use for every inline link inside page content
 * (never for nav, buttons, or the skip link, which have their own treatments).
 */
export const textLinkClass =
  'text-primary underline underline-offset-4 hover:text-primary-accent outline-none focus-visible:ring-3 focus-visible:ring-outline/50';
